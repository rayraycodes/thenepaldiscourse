# Copy to `.env.local` for local dev, or set in CI before `vite build`.
# GA4 Admin → Data streams → Web → Measurement ID
# Optional: overrides built-in GA4 id (G-0WS6E1VCHH) for staging or a different property
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
